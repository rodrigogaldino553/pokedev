<%= form_for :q, url: root_path, method: :get do |form| %>
  <%= form.text_field :username, placeholder: 'Type dev username...' %>
  <%= form.submit 'Catch it!' %>
<% end %>

<% if @dev.any? %>
  <%= image_tag @dev['avatar_url'] %>
<% end %>
